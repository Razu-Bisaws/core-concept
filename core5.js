// all Decelaration system demo

\//variable: let, const
let money = 20;
money = 12;
const name = 'kudus';

let isConfident = false;

const person = {
    name = 'dudos khan',
    age = 34,
    city = 'Dhaka'
}

const statement = `this person ${name} with age  ${person.age} `;

// condition 
if (money > 20 && age >= 34) {
    
} else {
    
}

/// array
 const numbers = [23, 34, 4566];
 const friends = ['ghj', 'tyd'];
//  object
 const products = [{name: 'laptop', price: 500}, {name: 'mobile phone', price: 300}, {name: 'tablet', price: 400}];

// loop: for loop
for (let i = 0; i < numbers.length; i++) {
    const number= numbers[i];
    
}

// function: regular arrow
function add (num1, num2){
    return num1 + num2;
}

const result = add(45, 23);

// function:  arrow function
const add2 = (num1, num2) => num1 + num2;
const fiveTimes = num => num * 5;
const doMath = (x, y) => {
    const sum = x + y;
    return sum * 2;
}

